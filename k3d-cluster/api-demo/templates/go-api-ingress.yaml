apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.go-api.name }}-ingress
  namespace: {{ .Values.go-api.namespace }}
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /songs
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.go-api.service.name }}
                port:
                  number: {{ .Values.go-api.service.port }}